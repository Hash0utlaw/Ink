"use client"

// Reserved for future client-side filter controls (price range slider, city input)
// Currently filters are handled via URL search params in the server page.
export function FlashFiltersBar() {
  return null
}
