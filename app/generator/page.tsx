import { redirect } from "next/navigation"

// Disabled pending launch — see app/generator/page.tsx.disabled for the
// full implementation to restore later.
export default function Page() {
  redirect("/")
}
